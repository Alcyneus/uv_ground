# aux_source_directory(. DIR_LIB_SRCS)
# include_directories(${PROJECT_SOURCE_DIR}/src/utils)
# #设置库文件输出路径
# set(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/test/lib)

# #生成库文件
# add_library(catch STATIC ${DIR_LIB_SRCS})
